# Git 仓库访问凭证 - SSH 密钥方式
#
# 使用方法：
# 1. 复制此文件为 repository-secret.yaml（已添加到 .gitignore）
# 2. 填入你的 SSH 私钥
# 3. kubectl apply -f bootstrap/repository-secret.yaml
#
# 注意：这个文件包含敏感信息，不应提交到 Git！

apiVersion: v1
kind: Secret
metadata:
  name: k8s-gitops-repo
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
type: Opaque
stringData:
  type: git
  url: git@github.com:SimpleLumine/k8s-gitops.git
  # 将你的 SSH 私钥粘贴在这里（保持正确的缩进）
  sshPrivateKey: |
    -----BEGIN OPENSSH PRIVATE KEY-----
    YOUR_SSH_PRIVATE_KEY_HERE
    -----END OPENSSH PRIVATE KEY-----
