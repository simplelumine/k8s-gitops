apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: open-webui
  namespace: flux-system
spec:
  interval: 10m
  dependsOn:
    - name: redis-operator
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./environments/apps/open-webui
  prune: true
  decryption:
    provider: sops
    secretRef:
      name: sops-age
