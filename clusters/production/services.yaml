apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: services
  namespace: flux-system
spec:
  dependsOn:
    - name: core
  interval: 10m
  path: ./clusters/production/overlays/services
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system-prod
  decryption:
    provider: sops
    secretRef:
      name: sops-age
