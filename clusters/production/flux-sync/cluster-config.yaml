apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cluster-config
  namespace: flux-system
spec:
  interval: 10m
  path: ./clusters/production/cluster-config
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system-prod
  decryption:
    provider: sops
    secretRef:
      name: sops-age
