apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: vikunja-db-secret-sync
  namespace: vikunja
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: postgres-store
    kind: ClusterSecretStore
  target:
    name: vikunja-db-secret
    template:
      engineVersion: v2
      data:
        DATABASE_URL: "postgres://{{ .username }}:{{ .password }}@postgres-rw.postgres.svc.cluster.local:5432/vikunja"
        username: "{{ .username }}"
        password: "{{ .password }}"
  data:
    - secretKey: username
      remoteRef:
        key: vikunja-db-secret
        property: username
    - secretKey: password
      remoteRef:
        key: vikunja-db-secret
        property: password
