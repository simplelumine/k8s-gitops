apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sillytavern-data
  namespace: sillytavern
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 15Gi
