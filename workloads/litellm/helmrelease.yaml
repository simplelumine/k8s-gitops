apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: litellm
  namespace: litellm
spec:
  interval: 1h
  chart:
    spec:
      chart: litellm-helm
      version: "1.82.3"
      sourceRef:
        kind: HelmRepository
        name: litellm
        namespace: litellm
      interval: 1h
  values:
    image:
      tag: v1.82.6  # PINNED: supply chain attack in v1.82.7+, do NOT upgrade until official all-clear
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
    db:
      deployStandalone: false
    redis:
      enabled: false
    serviceMonitor:
      enabled: false
