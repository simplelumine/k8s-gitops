apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Staging 环境 - 业务应用配置

# 所有资源部署到 staging namespace
namespace: staging

# 引用 base 配置
resources: []
  # - ../../base/litellm
  # - ../../base/litellm-postgres
  # - ../../base/open-webui

# 环境特定的补丁
# patches:
#   - path: litellm-patch.yaml
#     target:
#       kind: Deployment
#       name: litellm

# Staging 环境配置
# configMapGenerator:
#   - name: app-config
#     literals:
#       - environment=staging
#       - debug=true

# SOPS 加密的 Secret
# secretGenerator:
#   - name: app-secrets
#     files:
#       - secrets/database-credentials.enc.yaml
