apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Production 环境 - 核心基础设施配置

# 所有资源部署到 prod namespace
# namespace: prod

# 引用 base 配置
resources: []
  # - ../../base/cert-manager
  # - ../../base/longhorn
  # - ../../base/cloudnative-pg
  # - ../../base/redis-operator
  # - ../../base/tailscale-operator

# 环境特定的补丁
# patches:
#   - path: cert-manager-patch.yaml
#     target:
#       kind: HelmRelease
#       name: cert-manager

# 生产环境配置
# configMapGenerator:
#   - name: prod-config
#     literals:
#       - environment=production
#       - log_level=info

# SOPS 加密的 Secret
# secretGenerator:
#   - name: prod-secrets
#     files:
#       - secrets/example.enc.yaml
