name: Validate Manifests

on:
  pull_request:
    branches: [main]
    paths:
      - 'environments/**'
      - 'clusters/**'
      - '.github/workflows/validate-manifests.yaml'
  # Note: Removed push trigger to avoid duplicate validation after PR merge
  # Validation happens during PR review, no need to re-run after merge

jobs:
  kustomize:
    name: Kustomize Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Kustomize
        uses: imranismail/setup-kustomize@v2
        with:
          kustomize-version: '5.x'

      - name: Validate Core Components
        run: |
          echo "🔍 Validating core components..."
          for dir in environments/core/*/; do
            if [ -f "$dir/kustomization.yaml" ]; then
              echo "Validating $dir"
              kustomize build "$dir" > /dev/null
            fi
          done
          echo "✅ Core components validation complete"

      - name: Validate App Components
        run: |
          echo "🔍 Validating app components..."
          for dir in environments/apps/*/; do
            if [ -f "$dir/kustomization.yaml" ]; then
              echo "Validating $dir"
              kustomize build "$dir" > /dev/null
            fi
          done
          echo "✅ App components validation complete"

      - name: Validate Cluster Configurations
        run: |
          echo "🔍 Validating all cluster configurations..."
          for cluster in clusters/*/; do
            if [ -d "$cluster" ] && [ -f "$cluster/kustomization.yaml" ]; then
              echo "Validating $cluster"
              kustomize build "$cluster" > /dev/null
            fi
          done
          echo "✅ Cluster configurations validation complete"

  flux:
    name: Flux Validation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flux CLI
        uses: fluxcd/flux2/action@main

      - name: Validate Flux Manifests
        run: |
          echo "🔍 Validating Flux manifests with official CLI..."
          flux validate --path ./clusters/us-west
          echo "✅ Flux validation complete"

      - name: Check Flux Version
        run: flux version --client

  yaml-lint:
    name: YAML Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run yamllint
        uses: ibiqlik/action-yamllint@v3
        with:
          file_or_dir: environments/ clusters/
          config_data: |
            extends: default
            ignore: |
              clusters/*/flux-system/gotk-components.yaml
              clusters/*/flux-system/gotk-sync.yaml
              clusters/*/flux-system/kustomization.yaml
              **/*.sops.yaml
            rules:
              line-length: disable
              document-start: disable
              indentation:
                spaces: 2
              comments:
                min-spaces-from-content: 1
