name: Validate GitOps Manifests

on:
  pull_request:
    branches: [main]
    paths:
      - 'environments/**'
      - 'clusters/**'
      - '.github/workflows/validate.yaml'
  push:
    branches: [main]
    paths:
      - 'environments/**'
      - 'clusters/**'

jobs:
  validate-kustomize:
    name: Validate Kustomize Builds
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Kustomize
        uses: imranismail/setup-kustomize@v2
        with:
          kustomize-version: '5.x'

      - name: Validate Core Production
        run: |
          echo "🔍 Validating core production overlay..."
          kustomize build environments/core/overlays/prod

      - name: Validate Core Staging
        run: |
          echo "🔍 Validating core staging overlay..."
          kustomize build environments/core/overlays/staging

      - name: Validate Apps Production
        run: |
          echo "🔍 Validating apps production overlay..."
          kustomize build environments/apps/overlays/prod

      - name: Validate Apps Staging
        run: |
          echo "🔍 Validating apps staging overlay..."
          kustomize build environments/apps/overlays/staging

      - name: Validate Clusters us-west
        run: |
          echo "🔍 Validating clusters/us-west..."
          kustomize build clusters/us-west

      - name: Validate Clusters staging
        run: |
          echo "🔍 Validating clusters/staging..."
          kustomize build clusters/staging

  validate-flux:
    name: Validate Flux Resources
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate Flux Kustomization YAML Syntax
        run: |
          echo "🔍 Validating Flux Kustomization YAML syntax..."
          find clusters/ -name "*.yaml" -not -path "*/flux-system/gotk-*" -type f | while read file; do
            echo "Checking $file"
            # 简单的 YAML 语法检查
            python3 -c "import yaml; yaml.safe_load(open('$file'))" || exit 1
          done

  yaml-lint:
    name: YAML Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: YAML Lint
        uses: ibiqlik/action-yamllint@v3
        with:
          file_or_dir: environments/ clusters/
          config_data: |
            extends: default
            ignore: |
              clusters/*/flux-system/gotk-components.yaml
              clusters/*/flux-system/gotk-sync.yaml
            rules:
              line-length: disable
              indentation:
                spaces: 2
              comments:
                min-spaces-from-content: 1
