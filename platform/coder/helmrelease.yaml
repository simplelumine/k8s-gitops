apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: coder
  namespace: coder
spec:
  interval: 1h
  chart:
    spec:
      chart: coder
      version: "2.37.0"
      sourceRef:
        kind: HelmRepository
        name: coder
        namespace: coder
  values: {}
